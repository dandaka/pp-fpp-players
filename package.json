{
  "name": "pp-fpp-players",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun --hot packages/api/src/index.ts 2>&1 | sed 's/^/[api] /' & bun --cwd packages/web dev 2>&1 | sed 's/^/[web] /' & wait"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
